{
  "scripts": {
    "app": "npx shadow-cljs release app",
    "serverless": "npx shadow-cljs release serverless",
    "watch": "npx shadow-cljs watch app",
    "vercel-build": "npm run app && npm run serverless"
  },
  "devDependencies": {
    "shadow-cljs": "^2.19.2"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
